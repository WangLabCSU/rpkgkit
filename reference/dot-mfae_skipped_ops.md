# Operators and special forms that are never transformed

Operators and special forms that are never transformed

## Usage

``` r
.mfae_skipped_ops
```

## Format

An object of class `character` of length 35.
