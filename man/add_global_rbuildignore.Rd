% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17_add_global_rbuildignore.R
\name{add_global_rbuildignore}
\alias{add_global_rbuildignore}
\title{Add global .Rbuildignore patterns}
\usage{
add_global_rbuildignore(..., path = ".")
}
\arguments{
\item{...}{Additional regex patterns (character strings) to add beyond
the curated defaults. Each must already be in \code{.Rbuildignore} regex
format (e.g. \code{"^\\\\.myfile$"}).}

\item{path}{Character. Path to the package root directory. Defaults to
the current working directory (\code{"."}).}
}
\value{
Invisibly returns the path to \code{.Rbuildignore}.
}
\description{
Appends a curated set of common files and directories to \code{.Rbuildignore}
to exclude them from R package builds. This supplements the auto-generated
\code{.Rbuildignore} created by \code{usethis::create_package()}.

Already-present patterns are silently skipped. The curated set covers:
R build artifacts, Git files, IDE/dev tool directories, CI/CD config,
documentation build artifacts, CRAN/development files, and other common
files that should not be shipped with a package.
}
\examples{
\dontrun{
add_global_rbuildignore()

# With additional custom patterns
add_global_rbuildignore("^\\\\.myconfig$", "^data-raw$")
}
}
