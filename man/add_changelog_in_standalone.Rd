% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/23_add_changelog_in_standalone.R
\name{add_changelog_in_standalone}
\alias{add_changelog_in_standalone}
\title{Add or update a changelog entry in a standalone R file}
\usage{
add_changelog_in_standalone(path = NULL, description = NULL, date = NULL)
}
\arguments{
\item{path}{Character. Path to a standalone file, a directory, or \code{NULL}.
If \code{NULL} and \code{rstudioapi} is available, uses the currently active document.
If a directory, searches for standalone files matching the pattern.}

\item{description}{Character. Description of the changelog entry.
If \code{NULL}, an interactive prompt is used (not yet implemented).}

\item{date}{Character. Date string in \code{YYYY-MM-DD} format.
Defaults to today.}
}
\value{
Invisibly returns the path to the updated file.
}
\description{
Appends a new changelog entry at the top of the \code{Changelog} section in a
standalone file. Also updates the \code{last-updated} field in the YAML header
to today's date. If no \code{Changelog} section exists, one is created after
the YAML header.
}
\examples{
\dontrun{
add_changelog_in_standalone(tempdir(), "Added new feature")
}
}
