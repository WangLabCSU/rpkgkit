% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20A_package_convert_int_literals.R,
%   R/20_convert_int_literals.R
\name{package_convert_int_literals}
\alias{package_convert_int_literals}
\alias{convert_int_literals}
\title{Add Explicit Integer Suffix \code{L} to Integer Literals}
\usage{
package_convert_int_literals(path = NULL, dirs = c("R"), recursive = TRUE, ...)

convert_int_literals(path = NULL, verbose = TRUE, ...)
}
\arguments{
\item{path}{For \code{\link[=convert_int_literals]{convert_int_literals()}}, a character string specifying the
R file to modify. If \code{NULL} and RStudio is available, the currently active
document path is used.

For \code{\link[=package_convert_int_literals]{package_convert_int_literals()}}, a character string specifying the
package root. If \code{NULL}, the current working directory is used.}

\item{dirs}{Character vector of subdirectories relative to \code{path} to search.
Used only by \code{\link[=package_convert_int_literals]{package_convert_int_literals()}}. Defaults to
\code{c("R")}.}

\item{recursive}{Logical; recurse into subdirectories. Used only by
\code{\link[=package_convert_int_literals]{package_convert_int_literals()}}. Default \code{TRUE}.}

\item{...}{Additional arguments. Currently unused and must be empty.}

\item{verbose}{Logical; enable or disable per-file messages. Used only by
\code{\link[=convert_int_literals]{convert_int_literals()}}. Default \code{TRUE}.}
}
\value{
\code{\link[=convert_int_literals]{convert_int_literals()}} invisibly returns the modified file path.
\code{\link[=package_convert_int_literals]{package_convert_int_literals()}} invisibly returns a character vector of
modified file paths.
}
\description{
Converts bare integer literals to explicit integer form with an \code{L} suffix,
e.g. \code{seq_len(10)} becomes \code{seq_len(10L)}. Strings and comments are left
unchanged.

\code{\link[=convert_int_literals]{convert_int_literals()}} operates on one R file. When \code{path} is \code{NULL} and
RStudio is available, it uses the currently active document.
\code{\link[=package_convert_int_literals]{package_convert_int_literals()}} walks selected directories of an R package
and applies the same conversion to every \code{.R} / \code{.r} file found, except
automatically generated \code{RcppExports.R} files.
}
\details{
A token is treated as an integer literal when it is:
\itemize{
\item a decimal integer (\code{10}, \code{0}, \code{-} is not part of the token), or
\item a hexadecimal integer (\code{0xFF}, \code{0X10}),
}

and it is \strong{not}:
\itemize{
\item already suffixed with \code{L} / \code{l},
\item a floating-point number (\code{10.}, \code{1.0}, \code{.5}),
\item scientific notation (\code{1e5}, \code{1E-3}),
\item a complex literal (\code{10i}),
\item adjacent to an identifier character (\verb{a-zA-Z0-9._}).
}
}
\examples{
\donttest{
# --- Single file ---
temp <- tempfile(fileext = ".R")
writeLines("tmp <- seq_len(10)", temp)
convert_int_literals(temp)
readLines(temp)
# "tmp <- seq_len(10L)"

# --- Entire package ---
tmp_pkg <- tempdir()
usethis::create_package(tmp_pkg, open = FALSE)
writeLines("foo <- seq_len(42)", file.path(tmp_pkg, "R/foo.R"))
package_convert_int_literals(tmp_pkg)
readLines(file.path(tmp_pkg, "R/foo.R"))
}

}
