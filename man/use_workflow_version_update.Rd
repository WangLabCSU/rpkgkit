% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/42_use_workflow_version_update.R
\name{use_workflow_version_update}
\alias{use_workflow_version_update}
\title{Create a GitHub Actions workflow to auto-update R package version}
\usage{
use_workflow_version_update(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Character. Path to the package root directory. Defaults to the
current working directory (\code{"."}).}

\item{overwrite}{Logical. If \code{TRUE}, overwrite an existing workflow file.
Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the path to the created workflow file.
}
\description{
Copies the built-in \code{version_update.yml} workflow template to the target
package's \verb{.github/workflows/} directory. The workflow automatically bumps
the R package version based on commit messages, or manually via
\code{workflow_dispatch} with a specified version type.

Version bump rules (from commit messages, case-insensitive):
\itemize{
\item \strong{major} / \strong{breaking} - increments the major version (X.0.0)
\item \strong{feat} / \strong{feature} / \strong{minor} - increments the minor version (x.Y.0)
\item \strong{patch} / \strong{fix} / \strong{bug} - increments the patch version (x.y.Z)
\item Otherwise, no version bump occurs
}

The \code{workflow_dispatch} input always overrides commit message detection.
}
\examples{
\dontrun{
temp <- tempdir()
usethis::create_package(temp)
use_workflow_version_update(temp)
use_workflow_version_update(temp, overwrite = TRUE)
}
}
