% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_rename_func.R
\name{rename_func}
\alias{rename_func}
\title{Rename Function Definitions in an R File}
\usage{
rename_func(
  path = NULL,
  style = c("snake_case", "camelCase", "PascalCase", "google"),
  ...
)
}
\arguments{
\item{path}{A character string specifying the path to the R file to modify.
If \code{NULL} and RStudio is available, the currently active document path is used.}

\item{style}{Naming convention to apply. One of:
\itemize{
\item \code{"snake_case"}: all lowercase with underscores (e.g., \code{my_function})
\item \code{"camelCase"}: lower camel case (e.g., \code{myFunction})
\item \code{"PascalCase"}: upper camel case (e.g., \code{MyFunction})
\item \code{"google"}: dot-separated lowercase (e.g., \code{my.function})
}}

\item{...}{Additional arguments. Currently unused and must be empty.}
}
\value{
Invisibly returns the path to the modified file.
}
\description{
Renames function definitions in an R file to follow a consistent naming
convention. Supports \code{"snake_case"}, \code{"camelCase"}, \code{"PascalCase"}, and
\code{"google"} (dot-separated) naming styles. All references to the renamed
functions within the file are also updated.
}
\details{
Function definitions are identified using the pattern
\verb{name <- function(}, \verb{name = function(}, or the R 4.1+ shorthand
\verb{name <- \\\\(} / \verb{name = \\\\(}. Both the definition site and all
call sites / references within the file are updated. The conversion handles
mixed existing styles (snake_case, camelCase, PascalCase, dot.separated)
and normalizes function names to the target style.
}
\examples{
\donttest{
temp <- tempfile(fileext = ".R")
writeLines("foo_bar <- function(){message('foo_bar')}", temp)
rename_func(temp, style = "camelCase")
readLines(temp)
rename_func(temp, style = "snake_case")
readLines(temp)
}

}
