% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_flir_fix.R
\name{flir_fix}
\alias{flir_fix}
\title{Fix R code or package using flir}
\usage{
flir_fix(path = NULL, ...)
}
\arguments{
\item{path}{A file path, package directory path, or NULL. If NULL and
running in RStudio, uses the active document path.}

\item{...}{Additional arguments passed to the underlying flir fix function.}
}
\value{
Invisibly returns the result from the called flir function.
}
\description{
Automatically detect the type of path (file, package, or directory) and
apply the appropriate flir fix function.
}
\details{
The function determines the fix strategy based on the path type:
\itemize{
\item If \code{path} points to an existing file, calls \code{flir::fix()}
\item If \code{path} is a package directory (contains DESCRIPTION), calls \code{flir::fix_package()}
\item If \code{path} is a directory, calls \code{flir::fix_dir()}
}
}
\examples{
\dontshow{if (rlang::is_installed("flir")) withAutoprint(\{ # examplesIf}
tmp <- tempfile(fileext = ".R")
writeLines("a<-1+1", tmp)
flir_fix(tmp)
cat(readLines(tmp, warn = FALSE), sep = "\n")
\dontshow{\}) # examplesIf}
}
