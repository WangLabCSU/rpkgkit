% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/21_create_standalone.R
\name{create_standalone}
\alias{create_standalone}
\title{Creates a new standalone R script with YAML metadata header.
If \code{path} is an R package, the file is created in the \verb{R/} subdirectory.}
\usage{
create_standalone(
  standalone_name = NULL,
  path = NULL,
  standalone_head = list(license = "https://unlicense.org", imports = NULL, dependency =
    NULL, description = "This file provides..."),
  open = TRUE,
  ...
)
}
\arguments{
\item{standalone_name}{Character. The name suffix for the standalone file
(e.g., "my_utils" creates "standalone-my_utils.R").}

\item{path}{Character. Directory path where to create the file.
Defaults to the current RStudio document directory or ".".}

\item{standalone_head}{List. Metadata for the file header with elements:
\itemize{
\item \code{license}: Character. License URL or identifier.
Defaults to "https://unlicense.org".
\item \code{imports}: Character vector. Package dependencies to import.
Defaults to NULL.
\item \code{dependency}: Character vector. Hard dependencies for the
standalone file. Defaults to NULL.
\item \code{description}: Character. A short description of the standalone
file. Defaults to "To be filled.".
}}

\item{open}{Logical. Whether to open the file in RStudio editor.
Defaults to TRUE.}

\item{...}{Additional arguments (must be empty).}
}
\value{
Invisibly returns the path to the created file.
}
\description{
Creates a new standalone R script with YAML metadata header.
If \code{path} is an R package, the file is created in the \verb{R/} subdirectory.
}
\examples{
\dontrun{
create_standalone("my_utils", path = tempdir())
}
}
