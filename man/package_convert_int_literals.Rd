% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20A_package_convert_int_literals.R
\name{package_convert_int_literals}
\alias{package_convert_int_literals}
\title{Apply \code{convert_int_literals()} to an R Package}
\usage{
package_convert_int_literals(
  path = NULL,
  dirs = c("R", "tests"),
  recursive = TRUE,
  ...
)
}
\arguments{
\item{path}{Character path to the package root. If \code{NULL} and RStudio is
available, the active document's project / working directory is used
only when it looks like a package root (\code{DESCRIPTION} present).}

\item{dirs}{Character vector of subdirectories relative to \code{path} to
search. Defaults to \code{c("R", "tests")}.}

\item{recursive}{Logical; recurse into subdirectories. Default \code{TRUE}.}

\item{...}{Additional arguments. Currently unused and must be empty.}
}
\value{
Invisibly returns a character vector of modified file paths.
}
\description{
Walks the \verb{R/} and \verb{tests/} directories of an R package and runs
\code{\link[=convert_int_literals]{convert_int_literals()}} on every \code{.R} / \code{.r} file.
}
\examples{
\donttest{
tmp_pkg <- tempdir()
usethis::create_package(tmp_pkg, open = FALSE)
writeLines("foo <- seq_len(42)", file.path(tmp_pkg, "R/foo.R"))
package_convert_int_literals(tmp_pkg)
message(readLines(file.path(tmp_pkg, "R/foo.R")))
}

}
