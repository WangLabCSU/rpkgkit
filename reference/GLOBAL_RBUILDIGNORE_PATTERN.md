# Global `.Rbuildignore` patterns

Default regular-expression patterns used by
[`add_global_rbuildignore()`](https://wanglabcsu.github.io/rpkgkit/reference/add_global_rbuildignore.md).

## Usage

``` r
GLOBAL_RBUILDIGNORE_PATTERN
```
