% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_convert_func_syntax.R
\name{convert_func_syntax}
\alias{convert_func_syntax}
\title{Switch Between Explicit \verb{function()} and Implicit \verb{\\()} Syntax}
\usage{
convert_func_syntax(
  path = NULL,
  direction = c("to_lambda", "to_explicit"),
  ...
)
}
\arguments{
\item{path}{A character string specifying the path to the R file to modify.
If \code{NULL} and RStudio is available, the currently active document path is used.}

\item{direction}{Conversion direction. One of:
\itemize{
\item \code{"to_lambda"}: convert \verb{function(...)} to \verb{\\(...)}
\item \code{"to_explicit"}: convert \verb{\\(...)} to \verb{function(...)}
}}

\item{...}{Additional arguments. Currently unused and must be empty.}
}
\value{
Invisibly returns the path to the modified file.
}
\description{
Converts all function definitions in an R file between the explicit
\verb{function()} syntax and the R 4.1+ concise lambda \verb{\\()} syntax.
Handles strings and comments correctly, and supports nested function
definitions via iterative passes.
}
\details{
The conversion correctly handles strings, comments, and nested parentheses
in default argument values (e.g., \verb{function(x = foo(y))}).

Nested function definitions (e.g., \verb{function(x, f = function(y) ...)}) are
converted in multiple iterative passes, so all nesting levels are reached.
}
\examples{
\donttest{
temp <- tempfile(fileext = ".R")
writeLines("add_one <- function(x) x + 1", temp)
convert_func_syntax(temp, direction = "to_lambda")
readLines(temp)
# "add_one <- \\(x) x + 1"
convert_func_syntax(temp, direction = "to_explicit")
readLines(temp)
# "add_one <- function(x) x + 1"
}

}
