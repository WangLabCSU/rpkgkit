% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/47_convert_nonascii_code.R
\name{convert_nonascii_code}
\alias{convert_nonascii_code}
\title{Convert Non-ASCII Code}
\usage{
convert_nonascii_code(code, ..., reverse = FALSE, overwrite = NULL)
}
\arguments{
\item{code}{An R expression or a file path (character). When a bare
expression is supplied, it is captured via NSE and deparsed. When a
character string that points to an existing file is supplied, the
file content is read and converted.}

\item{...}{Additional arguments (must be empty).}

\item{reverse}{Logical. If \code{TRUE}, converts \verb{\\uXXXX} escape sequences back
to readable Unicode characters. If \code{FALSE} (default), encodes non-ASCII
characters to \verb{\\uXXXX} escapes.}

\item{overwrite}{Logical. Only used when \code{code} is a file path. If \code{TRUE},
overwrites the file with the converted content. Default is \code{NULL}, which
prompts the user interactively.}
}
\value{
Invisibly returns the converted code as a character string.
If \code{code} is a file path and \code{overwrite = TRUE}, the file is updated
in place and the function returns the path invisibly.
}
\description{
Converts non-ASCII characters in R code to their ASCII escape sequences
(e.g., \verb{\\uXXXX}), or restores ASCII escape sequences back to readable
characters. Accepts either an R expression (via NSE) or a file path
(as a character string).
}
\examples{
\donttest{
# Convert non-ASCII characters in a bare expression to \\u escapes
convert_nonascii_code(print('\u4e2d\u6587'))

# Reverse: restore \\u escapes to readable characters
convert_nonascii_code(print('\u4e2d\u6587'), reverse = TRUE)
}

}
