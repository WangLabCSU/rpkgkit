% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/44_use_multilanguage_readme.R
\name{use_multilanguage_readme}
\alias{use_multilanguage_readme}
\title{Create Multilingual README Files}
\usage{
use_multilanguage_readme(
  lang = c("zh-cn", "es", "fr", "ar", "ru"),
  ...,
  path = ".",
  overwrite = FALSE
)
}
\arguments{
\item{lang}{Character vector of language codes (e.g. \code{"zh-cn"}, \code{"ja"}).
Defaults to the five non-English UN official languages. Codes must be
supported by the internal name mapping (see \strong{Language Codes} below)
or they will be used as-is for badge labels.}

\item{...}{Not used.}

\item{path}{Character. Path to the package root directory. Defaults to
the current working directory (\code{"."}).}

\item{overwrite}{Logical. If \code{TRUE}, overwrite existing README translation
files. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns a character vector of paths to the created files.
}
\description{
Creates translated README files under \verb{inst/} for a target R package and
prints badges that can be pasted into the main \code{README.md} to link to each
translation.

The five non-English United Nations official languages are used by
default: Chinese (\code{zh-cn}), Spanish (\code{es}), French (\code{fr}),
Arabic (\code{ar}), and Russian (\code{ru}). Any language code (including
non-UN languages) can be supplied via the \code{lang} argument.
}
\section{Language Codes}{

The following codes have built-in display name mappings:\tabular{ll}{
   Code \tab Display Name \cr
   \code{en} \tab English \cr
   \code{zh-cn} \tab 简体中文 \cr
   \code{zh-tw} \tab 繁體中文 \cr
   \code{es} \tab Español \cr
   \code{fr} \tab Français \cr
   \code{de} \tab Deutsch \cr
   \code{pt} \tab Português \cr
   \code{ja} \tab 日本語 \cr
   \code{ko} \tab 한국어 \cr
   \code{ar} \tab العربية \cr
   \code{ru} \tab Русский \cr
   \code{it} \tab Italiano \cr
   \code{nl} \tab Nederlands \cr
   \code{pl} \tab Polski \cr
   \code{tr} \tab Türkçe \cr
   \code{vi} \tab Tiếng Việt \cr
   \code{th} \tab ไทย \cr
   \code{id} \tab Bahasa Indonesia \cr
   \code{hi} \tab हिन्दी \cr
}


Unrecognised codes are used verbatim as badge labels.
}

\examples{
\dontrun{
dir <- tempdir()
usethis::create_package(dir)
use_multilanguage_readme(path = dir)

# Custom languages
use_multilanguage_readme(c("de", "ja", "ko"), path = dir)
}
}
