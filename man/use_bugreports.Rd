% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/50_use_bugreports.R
\name{use_bugreports}
\alias{use_bugreports}
\title{Add a \code{BugReports} field to a package's \code{DESCRIPTION}}
\usage{
use_bugreports(url = NULL, path = NULL, ...)
}
\arguments{
\item{url}{Character. The bug reports URL (usually the GitHub issues
page). If \code{NULL} (the default), it is derived from the \code{git} remote
configuration by appending \verb{/issues} to the repository URL.}

\item{path}{Path to the package root. If \code{NULL} (the default), the current
working directory is used.}

\item{...}{Must be empty. Reserved for future arguments.}
}
\value{
Invisibly returns the URL that was written.
}
\description{
Sets the \code{BugReports} field of a package's \code{DESCRIPTION} file. By default,
the GitHub issues page URL is derived from the configured \code{git} remote
(\code{origin}).
\itemize{
\item Requires that the target directory is an R package root (contains a
\code{DESCRIPTION} file).
\item Both \verb{https://} and \verb{git@} style remotes are supported (see \code{\link[=use_url]{use_url()}}).
}
}
\examples{
\donttest{
tmpdir <- tempdir()
usethis::create_package(path = tmpdir)
use_bugreports(
  url = "https://github.com/WangLabCSU/rpkgkit/issues",
  path = tmpdir
)
}
}
