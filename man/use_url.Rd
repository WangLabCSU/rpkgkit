% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/49_use_url.R
\name{use_url}
\alias{use_url}
\title{Add a \code{URL} field to a package's \code{DESCRIPTION}}
\usage{
use_url(url = NULL, pkgdown_url = NULL, path = NULL, ...)
}
\arguments{
\item{url}{Character. The primary URL (usually the GitHub repository).
If \code{NULL} (the default), it is detected from the \code{git} remote
configuration.}

\item{pkgdown_url}{Character. An optional pkgdown website URL, appended
after \code{url}.}

\item{path}{Path to the package root. If \code{NULL} (the default), the current
working directory is used.}

\item{...}{Must be empty. Reserved for future arguments.}
}
\value{
Invisibly returns the URLs that were written.
}
\description{
Sets the \code{URL} field of a package's \code{DESCRIPTION} file. By default, the
GitHub repository URL is detected from the configured \code{git} remote
(\code{origin}). An additional pkgdown website URL can optionally be appended.
\itemize{
\item Requires that the target directory is an R package root (contains a
\code{DESCRIPTION} file).
\item Both \verb{https://} and \verb{git@} style remotes are converted to a browser
URL (a trailing \code{.git} suffix is removed).
}
}
\examples{
\donttest{
tmpdir <- tempdir()
usethis::create_package(path = tmpdir)
use_url(
  url = "https://github.com/WangLabCSU/rpkgkit",
  pkgdown_url = "https://wanglabcsu.github.io/rpkgkit/",
  path = tmpdir
)
}
}
