% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/48_convert_knitr_chunk_header.R
\name{convert_knitr_chunk_header}
\alias{convert_knitr_chunk_header}
\title{Convert Knitr Chunk Headers in R Markdown Files}
\usage{
convert_knitr_chunk_header(path = NULL, ...)
}
\arguments{
\item{path}{Path to an R package root directory or an R Markdown file.
If \code{NULL} (the default), the current working directory is used.}

\item{...}{Additional arguments passed to \code{knitr::convert_chunk_header()}.}
}
\value{
Invisibly returns a character vector of file paths that were
converted.
}
\description{
Converts legacy knitr chunk headers in R Markdown (\code{.Rmd}) files to the
current header syntax using \code{knitr::convert_chunk_header()}.

When \code{path} points to an R package root (a directory containing a
\code{DESCRIPTION} file), the conversion is applied to all \code{.Rmd} files found
under the package \verb{vignettes/} directory (including subdirectories) and to
the package \code{README.Rmd} file when present.

When \code{path} points to a single file, that file is converted and written
back in place.
}
\examples{
\dontshow{if (rlang::is_installed("knitr")) withAutoprint(\{ # examplesIf}
tmp <- tempfile(fileext = ".Rmd")
writeLines(c("```{r, echo=TRUE, fig.width=10}", "x <- 1", "```"), tmp)
convert_knitr_chunk_header(tmp)
readLines(tmp)
\dontshow{\}) # examplesIf}
}
