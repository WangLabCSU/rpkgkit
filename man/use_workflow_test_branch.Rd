% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/42B_use_workflow_test_branch.R
\name{use_workflow_test_branch}
\alias{use_workflow_test_branch}
\title{Create a GitHub Actions Workflow to Sync Main to Test Branch}
\usage{
use_workflow_test_branch(path = NULL, overwrite = FALSE)
}
\arguments{
\item{path}{Character. Path to the package root directory. If \code{NULL}
(the default), uses the current working directory.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite existing workflow and action
files. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the path to the created workflow file.
}
\description{
Copies the built-in \code{sync_test_branch.yml} workflow template and the
corresponding \code{sync-test-branch} composite action to the target package's
\verb{.github/} directory. The workflow triggers on push to \code{main}/\code{master} and
force-pushes the main branch contents onto the \code{test} branch.

\strong{Safety mechanism}: If the \code{test} branch contains commits that are not
reachable from \code{main} (i.e., unmerged work-in-progress), the sync is
skipped to avoid overwriting unmerged changes.

This enables convenient isolated debugging: work on the \code{test} branch,
push experimental changes, and each push to \code{main} will reset \code{test}
to match \code{main} (as long as no unmerged work exists on \code{test}).
}
\examples{
\dontrun{
temp <- tempdir()
usethis::create_package(temp)
use_workflow_test_branch(temp)
use_workflow_test_branch(temp, overwrite = TRUE)
}
}
